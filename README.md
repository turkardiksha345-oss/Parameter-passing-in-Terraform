# Parameter-passing-in-Terraform
This repository contains Terraform modules for provisioning AWS infrastructure (VPC, subnets, EC2, security groups, etc.) by passing environment variables through parameters, enabling reusable, automated, and consistent Infrastructure as Code (IaC) deployments across multiple environments.
